defaults:
  - override /database@_global_: island_small  # use a small database config
  - override /evolution@_global_: low_rpm_config  # use low-RPM evolution config
  - override /task@_global_: circle_packing
  - override /cluster@_global_: local
  - _self_

variant_suffix: "_low_rpm"
